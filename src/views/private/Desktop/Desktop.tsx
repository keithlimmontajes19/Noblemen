import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const Desktop = (props: PropsType): ReactElement => {
  return <div>Desktop</div>;
};

export default Desktop;

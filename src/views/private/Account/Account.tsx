import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const Account = (props: PropsType): ReactElement => {
  return <>Account</>;
};

export default Account;

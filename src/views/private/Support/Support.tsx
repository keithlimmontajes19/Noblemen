import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const Support = (props: PropsType): ReactElement => {
  return <>Support</>;
};

export default Support;

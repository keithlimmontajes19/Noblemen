import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const Invoices = (props: PropsType): ReactElement => {
  return <>Invoices</>;
};

export default Invoices;

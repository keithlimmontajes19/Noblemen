import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const Mobile = (props: PropsType): ReactElement => {
  return <div>Mobile</div>;
};

export default Mobile;

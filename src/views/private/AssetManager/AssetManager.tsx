import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const AssetManager = (props: PropsType): ReactElement => {
  return <>AssetManager</>;
};

export default AssetManager;
